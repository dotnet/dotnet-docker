parameters:
  serviceConnection: ""
  channel: ""
  repo: "https://github.com/dotnet/dotnet"

steps:
- template: update-dependencies.yml
  parameters:
    args: "from-channel ${{ parameters.channel }} ${{ parameters.repo }}"
    useInternalBuild: false
    serviceConnection: ${{ parameters.serviceConnection }}
